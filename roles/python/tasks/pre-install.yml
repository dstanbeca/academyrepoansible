---
- name: ensure a list of packages installed 
#  command: yum groupinstall "Development Tools"
#  command: yum install bzip2-devel
#  command: yum install openssl-devel
#  command: yum install gcc
#  command: yum install libffi-devel
  yum:
    name: "{{ packages }}"
  vars:
    ansible_python_interpreter: /usr/bin/python2.7
    packages:
      - gcc
      - openssl-devel
      - bzip2-devel
      - libffi-devel
  become: yes

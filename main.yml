---
   - hosts: all
     become: true
     gather_facts: yes
     vars:
      - user: ansible

     tasks:
        - include: playbook_common.yml
        - include: playbook_usercreation.yml
        - include: playbook_dockerinstall.yml
